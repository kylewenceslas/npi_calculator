/*
import { render, screen } from '@testing-library/react';
import App from './App';


test('renders stack result', () => {
  render(<App />);
  const linkElement = screen.getByText(/stack/i);
  expect(linkElement).toBeInTheDocument();
});
*/